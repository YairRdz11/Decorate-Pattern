//The decorator class: It extends Pizza to be
//interchangeable with it toppling decorator can
//also be implemented as an interface
public abstract class ToppingDecorator
{
    public abstract  string GetDescription();
}